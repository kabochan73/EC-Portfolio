# ローカル開発用（next dev）。本番用は frontend/Dockerfile（docs/04）。
# node_modules はコンテナ内（名前付きボリューム）に置く。ソースはバインドマウント。
FROM node:22-alpine
WORKDIR /app
# 依存はコンテナ起動時に入れる（package.json 変更のたびに rebuild しなくて済む）
CMD ["sh", "-c", "npm install && npm run dev"]
